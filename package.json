{
  "name": "api-test-suite",
  "scripts": {
    "test": "mocha",
    "tokens": "nodemon --watch .env --watch ./tokens/server.js ./tokens/server.js"
  },
  "devDependencies": {
    "chai": "^6.2.2",
    "mocha": "^11.7.5"
  },
  "dependencies": {
    "path-browserify": "0dataapp/path-browserify-ecma"
  }
}
